name: Release

on:
  push:
    tags:
      - 'v*.*.*'
      - 'cli-v*.*.*'
      - 'mcp-v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Existing tag to publish (v0.2.0, cli-v0.2.0 or mcp-v0.2.0). Steps that already published are skipped.'
        required: true

permissions:
  contents: write
  id-token: write

jobs:
  extension:
    name: Publish VS Code Extension
    if: startsWith(inputs.tag || github.ref_name, 'v')
    runs-on: ubuntu-latest
    env:
      TAG: ${{ inputs.tag || github.ref_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: refs/tags/${{ env.TAG }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: apps/vscode/package.json

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: apps/vscode/.node-version
          cache: pnpm
          cache-dependency-path: apps/vscode/pnpm-lock.yaml

      - name: Setup just
        uses: extractions/setup-just@v2

      - name: Install dependencies
        run: just install

      - name: Quality gate
        run: just check

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Verify package.json version matches tag
        run: |
          PKG_VERSION=$(node -p "require('./apps/vscode/package.json').version")
          TAG_VERSION=${{ steps.version.outputs.VERSION }}
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "Version mismatch: package.json=$PKG_VERSION tag=$TAG_VERSION"
            exit 1
          fi

      - name: Package extension
        working-directory: apps/vscode
        run: pnpm exec vsce package --no-dependencies -o debuggatha-${{ steps.version.outputs.VERSION }}.vsix

      - name: Publish to VS Code Marketplace
        working-directory: apps/vscode
        run: pnpm exec vsce publish --skip-duplicate --no-dependencies --packagePath debuggatha-${{ steps.version.outputs.VERSION }}.vsix
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      - name: Publish to Open VSX Registry
        working-directory: apps/vscode
        run: pnpm dlx ovsx publish debuggatha-${{ steps.version.outputs.VERSION }}.vsix --skip-duplicate -p ${{ secrets.OVSX_PAT }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          files: |
            apps/vscode/debuggatha-${{ steps.version.outputs.VERSION }}.vsix
          generate_release_notes: true
          body: |
            ## Debuggatha v${{ steps.version.outputs.VERSION }}

            See [CHANGELOG.md](https://github.com/sxnnyside-project/debuggatha/blob/main/CHANGELOG.md) for details.

            ### Install
            Search **Debuggatha** in the VS Code Extensions panel, or download the `.vsix` from this release and install manually:

            ```
            code --install-extension debuggatha-${{ steps.version.outputs.VERSION }}.vsix
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  cli:
    name: Publish CLI
    if: startsWith(inputs.tag || github.ref_name, 'cli-v')
    runs-on: ubuntu-latest
    env:
      TAG: ${{ inputs.tag || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: refs/tags/${{ env.TAG }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Setup just
        uses: extractions/setup-just@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Verify package version matches tag
        run: |
          PKG_VERSION=$(node -p "require('./packages/cli/package.json').version")
          TAG_VERSION=${TAG#cli-v}
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "Version mismatch: package.json=$PKG_VERSION tag=$TAG_VERSION"
            exit 1
          fi

      - name: Build, test, and smoke-test the installed package
        run: |
          bun run test
          just smoke

      - name: Publish to npm
        working-directory: packages/cli
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Build standalone binaries
        run: just binaries

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          files: dist/binaries/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  mcp:
    name: Publish MCP server
    if: startsWith(inputs.tag || github.ref_name, 'mcp-v')
    runs-on: ubuntu-latest
    env:
      TAG: ${{ inputs.tag || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: refs/tags/${{ env.TAG }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Setup just
        uses: extractions/setup-just@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Verify package version matches tag
        run: |
          PKG_VERSION=$(node -p "require('./packages/mcp/package.json').version")
          TAG_VERSION=${TAG#mcp-v}
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "Version mismatch: package.json=$PKG_VERSION tag=$TAG_VERSION"
            exit 1
          fi

      - name: Build, test, and smoke-test the installed package
        run: |
          bun run test
          just smoke

      - name: Publish to npm
        working-directory: packages/mcp
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
